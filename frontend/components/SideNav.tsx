import { FC } from "react";

const SideNav: FC = () => {
    return (
        <nav className="fixed h-screen top-12 w-64 border-solid border-r border-gray-300 bg-white"></nav>
    );
};

export default SideNav;
