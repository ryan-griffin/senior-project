import { FC } from "react";

const SideNav: FC = () => {
    return (
        <nav className="fixed h-screen top-14 w-64 border-solid border-r border-gray-500"></nav>
    );
};

export default SideNav;
